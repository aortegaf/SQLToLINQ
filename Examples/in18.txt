SELECT
    ORDERS.STATE
FROM
    ORDERS
JOIN DELIVERY ON
ORDERS.ORDERID = DELIVERY.ORDERID
WHERE DELIVERY.STATE = 'Delivered' && ORDERS.ORDERID = 10
GROUP BY ORDERS.STATE, ORDERS.DATE
ORDER BY ORDERS.STATE;